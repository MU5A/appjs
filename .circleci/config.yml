
version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:12.22.9
      - image: cimg/base:stable

    working_directory: ~/repo

    # Add steps to the job
    steps:
      - run: npm install
      - run: npm test

